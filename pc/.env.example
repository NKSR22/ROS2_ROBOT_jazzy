# คัดลอกไฟล์นี้เป็น .env แล้วปรับค่าตามสภาพแวดล้อมจริง
# cp .env.example .env

# ROS 2 Domain ID (ต้องตรงกันทั้ง PC และ Pi)
ROS_DOMAIN_ID=30

# X11 Display สำหรับ GUI (ตรวจสอบด้วย: echo $DISPLAY)
DISPLAY=:0
